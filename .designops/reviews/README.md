# Human reviews

**Status:** No current signed strategy, direction, handoff, or release review.

The owner implementation exception unblocks the scoped local-first Expo MVP under `apps/mobile/` without those signatures. Release remains fail-closed: it still needs signed handoff and release approvals plus current iOS/Android native verification evidence. Do not invent a signed review.

## Superseded reviews

`strategy.superseded-2026-07-20.json` preserves the prior signed Strategy attestation. Its digest became stale when the owner-authorized simulation-backed MVP package changed the evidence registry. It is historical only and is intentionally not used by the active gate.
