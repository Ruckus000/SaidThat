# DesignOps status

Read `AGENTS.md` and the current `.designops/project.json`, then run:

```bash
node tools/designops/enforce.mjs --intent design --json
```

Summarize the current workflow phase, gate result, missing approval or evidence, and which categories of work are permitted. Do not modify files, draft approvals, or sign anything.

